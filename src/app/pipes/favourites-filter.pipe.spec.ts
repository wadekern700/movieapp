import { FavouritesFilterPipe } from './favourites-filter.pipe';

describe('FavouritesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FavouritesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
